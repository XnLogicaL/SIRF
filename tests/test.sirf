global @main;

data .LC0: 72, 101, 108, 108, 111, 44, 32, 119, 111, 114, 108, 100, 10, 0;

function int32 @add (int32, int32) {
  mov r0, int32(34);  
  add r0, int32(35);
  ret r0;
}

function int32 @main () {
  ret int32(0);
}
